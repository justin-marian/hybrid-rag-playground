# Backend URL, only used for the Vite dev-server proxy.
#! In production, leave unset and serve the API and web app from the same origin.
AUTO_RESEARCH_CONFIG=./configs/auto_research.yml
VITE_API_TARGET=http://localhost:8080
RUN_RAG=true # true - retrieval+indexing+generation / false - retrieval+indexing
UV_LINK_MODE=copy
HF_TOKEN=hf_
